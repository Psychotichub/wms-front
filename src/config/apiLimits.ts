/** Max `limit` allowed by GET /api/reports/daily */
export const DAILY_REPORTS_MAX_LIMIT = 10000;

/** Page size for daily report list (single-day view) */
export const DAILY_REPORTS_PAGE_SIZE = 150;

/** Batch size when scanning a date range (e.g. Price screen) */
export const DAILY_REPORTS_RANGE_PAGE_SIZE = 500;
